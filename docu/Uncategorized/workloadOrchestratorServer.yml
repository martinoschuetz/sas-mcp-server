---
openapi: 3.0.3
info:
  title: SAS Workload Orchestrator Server
  version: "1"
  description: |-
    The SAS Workload Orchestrator Server API is an internal API that
    enables the manager pods to talk to the server pods to perform
    various operations. This API is not intended for public use.
  termsOfService: "https://www.sas.com/en_us/legal/copyright.html"
  contact:
    name: SAS Developers
    url: https://developer.sas.com
    email: devex@sas.com
servers:
- url: http://example.com/workloadOrchestratorServer
  description: Kubernetes mock server
tags:
- name: Root
  description: Contains the operations for the root resource of the SAS Workload Orchestrator
    Server API.
- name: DaemonToManager
  description: Contains the operations sent to a server from the current manager.
- name: ManagerToDaemon
  description: Contains the operations sent to the current manager from a server.
paths:
  /:
    get:
      summary: Get API resource links
      description: Returns a list of links to the top-level resources in this API.
      operationId: root
      tags:
      - Root
      responses:
        "200":
          description: The request succeeded.
          content:
            application/vnd.sas.api+json:
              schema:
                $ref: "#/components/schemas/api"
              examples:
                api:
                  $ref: "#/components/examples/apiInfoResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/api"
              examples:
                api:
                  $ref: "#/components/examples/apiInfoResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons:
    head:
      summary: Is daemon available
      description: Used by manager to determine if the daemon is available. A 200
        response indicates that the daemon is available, while a 406 response indicates
        that it is not.
      operationId: headersForIsDaemonAvailable
      tags:
      - ManagerToDaemon
      responses:
        "200":
          description: The request succeeded.
          headers:
            Content-Type:
              schema:
                type: string
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/config#setGridConfiguration:
    put:
      summary: Update the grid configuration on the server
      description: Updates the grid configuration to the newly specified configuration.
      operationId: updateGridConfig
      tags:
      - ManagerToDaemon
      requestBody:
        required: true
        content:
          application/vnd.sas.sasgrid.daemon.config+json;version=1;charset=utf-8:
            schema:
              $ref: "#/components/schemas/daemonGridConfig"
            examples:
              daemonGridConfig:
                $ref: "#/components/examples/daemonGridConfig"
      responses:
        "204":
          description: Grid updated successfully and nothing was returned.
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/config#setTenantConfiguration:
    put:
      summary: Update the tenant configuration on the server
      description: Updates the tenant configuration for the administrator's tenant.
        This is necessary in a multi-tenant environment because the user names and
        groups are only available for an administrator belonging to the same tenant.
      operationId: updateTenantConfig
      tags:
      - ManagerToDaemon
      requestBody:
        required: true
        content:
          application/vnd.sas.sasgrid.grid.tenant.config+json;version=1;charset=utf-8:
            schema:
              $ref: "#/components/schemas/tenantConfig"
            examples:
              tenantConfig:
                $ref: "#/components/examples/tenantConfig"
      responses:
        "204":
          description: Grid updated successfully and nothing was returned.
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/hosts:
    post:
      summary: Add a host to the grid. Handled by the current manager
      description: Adds a host to the grid with the provided information. This should
        be called by a server when it starts up to add itself to the grid and provide
        its information to the manager.
      operationId: createDaemonHost
      tags:
      - DaemonToManager
      requestBody:
        required: true
        content:
          application/vnd.sas.sasgrid.daemon.host.info+json;version=1:
            schema:
              $ref: "#/components/schemas/daemonHostInfo"
            examples:
              daemonHostInfo:
                $ref: "#/components/examples/daemonHostInfo"
      responses:
        "204":
          description: Host added successfully
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/hosts/{hostName}:
    put:
      summary: Update daemon host information
      description: Updates the information about a host in the grid. This should be
        called by a server periodically to update the manager with the latest information
        about the host it is running on.
      operationId: updateDaemonHost
      tags:
      - DaemonToManager
      parameters:
      - name: hostName
        description: The name of the host to update.
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/vnd.sas.sasgrid.daemon.host.state+json;version=1:
            schema:
              $ref: "#/components/schemas/daemonHostState"
            examples:
              daemonHostState:
                $ref: "#/components/examples/daemonHostState"
      responses:
        "204":
          description: Host updated successfully
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/jobs:
    post:
      summary: Start a job on the daemon
      description: Starts a job on the daemon with the provided job information. This
        should be called by the manager to start a job on the server.
      operationId: createDaemonJob
      tags:
      - ManagerToDaemon
      requestBody:
        required: true
        content:
          application/vnd.sas.sasgrid.job+json;version=1:
            schema:
              $ref: "#/components/schemas/jobInfo"
            examples:
              jobInfo:
                $ref: "#/components/examples/jobInfo"
      responses:
        "204":
          description: Job started successfully
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/jobs/{jobId}:
    head:
      summary: Check if job exists on server
      description: Checks if a job with the specified ID exists on the server. This
        can be used by the manager to check if a job is still running on the server.
      operationId: headersForDaemonJob
      tags:
      - ManagerToDaemon
      parameters:
      - name: jobId
        description: The ID of the job to check.
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Job exists
          headers:
            Content-Type:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
    put:
      summary: Update job information
      description: Updates the information on the manager about a job running on the
        server. This should be called by the server periodically to update the manager
        with the latest information about a job that is running.
      operationId: updateDaemonJob
      tags:
      - DaemonToManager
      parameters:
      - name: jobId
        description: The ID of the job to update.
        in: path
        required: true
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/vnd.sas.sasgrid.job+json;version=1:
            schema:
              $ref: "#/components/schemas/jobInfo"
            examples:
              jobInfo:
                $ref: "#/components/examples/jobInfo"
      responses:
        "204":
          description: Job updated successfully
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/jobs/{jobId}/state:
    put:
      summary: Set daemon job state
      description: Sets the state of a job running on the server. This should be called
        by the manager to set the state of a job (e.g., cancel a running job). The
        server should also call this endpoint to update the manager on any state changes
        that occur on the server side (e.g., job completed).
      operationId: updateDaemonJobState
      tags:
      - ManagerToDaemon
      parameters:
      - name: jobId
        description: The ID of the job to update the state of.
        in: path
        required: true
        schema:
          type: string
      - name: value
        description: The new state of the job (e.g., running, completed, cancelled,
          etc.).
        in: query
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Job state updated
          content:
            application/vnd.sas.sasgrid.job+json;version=1:
              schema:
                $ref: "#/components/schemas/jobInfo"
              examples:
                jobInfo:
                  $ref: "#/components/examples/jobInfo"
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/loggers:
    get:
      summary: Get daemon loggers information
      description: Retrieves the current loggers and their levels from the daemon.
        This should be called by the manager to get the current loggers information
        from the server.
      operationId: getDaemonLoggers
      tags:
      - ManagerToDaemon
      responses:
        "200":
          description: Loggers information
          content:
            application/vnd.sas.sasgrid.loggers.response+json;version=1:
              schema:
                $ref: "#/components/schemas/loggersLevelInfo"
              examples:
                loggersLevelInfo:
                  $ref: "#/components/examples/loggersLevelInfo"
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
    put:
      summary: Update daemon loggers configuration
      description: Updates the configuration of the loggers on the daemon. This should
        be called by the manager to update the loggers configuration on the server.
      operationId: updateDaemonLoggers
      tags:
      - ManagerToDaemon
      requestBody:
        required: true
        content:
          application/vnd.sas.sasgrid.loggers.request+json;version=1:
            schema:
              $ref: "#/components/schemas/loggersLevelRequest"
            examples:
              loggersLevelRequest:
                $ref: "#/components/examples/loggersLevelRequest"
      responses:
        "204":
          description: Loggers updated successfully
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/logs:
    get:
      summary: Get log information from the server or backup manager
      description: Retrieves the latest log output. This should be called by the manager
        to get the latest log information from the server. The manager can specify
        various parameters to filter the log output (e.g., logger name, log level,
        time range, etc.). This endpoint can also be used by a backup manager to get
        log information from the current manager for backup manager enumeration purposes.
      operationId: getLogInfo
      tags:
      - ManagerToDaemon
      parameters:
      - $ref: "#/components/parameters/logsLogger"
      - $ref: "#/components/parameters/logsLevel"
      - $ref: "#/components/parameters/logsThreshold"
      - $ref: "#/components/parameters/logsStartTime"
      - $ref: "#/components/parameters/logsEndTime"
      - $ref: "#/components/parameters/logsHost"
      - $ref: "#/components/parameters/collectionStart"
      - $ref: "#/components/parameters/collectionLimit"
      responses:
        "200":
          description: The request succeeded.
          content:
            application/vnd.sas.sasgrid.logs+json;version=1;charset=utf-8:
              schema:
                $ref: "#/components/schemas/logsInfo"
              examples:
                logs:
                  $ref: "#/components/examples/logsInfo"
            application/vnd.sas.collection+json;version=2;charset=utf-8:
              schema:
                $ref: "#/components/schemas/logsCollection"
              examples:
                logs:
                  $ref: "#/components/examples/logsCollection"
            application/json:
              schema:
                $ref: "#/components/schemas/logsCollection"
              examples:
                logs:
                  $ref: "#/components/examples/logsCollection"
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/manager:
    get:
      summary: Get current manager name
      description: Retrieves the name of the current manager. This should be called
        by the daemon to get the name of the manager it is currently associated with.
      operationId: getDaemonManager
      tags:
      - DaemonToManager
      responses:
        "200":
          description: Manager name
          content:
            text/plain:
              schema:
                type: string
              example: "sas-workload-orchestrator-1"
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/manager/{managerName}/state:
    put:
      summary: Set state of a manager candidate so that backup manager candidates
        know whether to include it in backup manager enumeration
      description: Sets the state of a manager candidate so that backup manager candidates
        know whether to include it in backup manager enumeration. This should be called
        by the manager to set its own state for backup manager enumeration purposes.
        The state can be set to "active" to indicate that this manager should be included
        in backup manager enumeration, or "inactive" to indicate that it should not
        be included in backup manager enumeration.
      operationId: updateDaemonManagerState
      tags:
      - ManagerToDaemon
      parameters:
      - name: managerName
        description: The name of the manager to update the state of.
        in: path
        required: true
        schema:
          type: string
      - name: value
        description: The state of the manager for backup manager enumeration purposes
          (e.g., active, inactive, etc.).
        in: query
        required: true
        schema:
          type: string
          enum: [active, inactive]
      responses:
        "204":
          description: Manager state updated successfully
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
  /daemons/scaling:
    put:
      summary: Set daemon scale-down prevention state. When enabled, the daemon will
        make sure something is always running
      description: Sets the daemon scale-down prevention state. When enabled, the
        daemon will make sure something is always running. This should be called by
        the manager to update the scaling configuration on the server.
      operationId: updateDaemonScaling
      tags:
      - ManagerToDaemon
      parameters:
      - name: value
        description: The scaling state to set on the daemon (e.g., enable or disable
          scale-down prevention).
        in: query
        required: true
        schema:
          type: string
          enum: ["enable", "disable"]
      responses:
        "204":
          description: Scaling configuration updated successfully
        "400":
          $ref: "#/components/responses/badHMACResponse"
        "406":
          $ref: "#/components/responses/notAcceptableResponse"
components:
  #--------------------------------------------------------------------
  #
  #                       PARAMETERS
  #
  #--------------------------------------------------------------------
  # parameters:

  #--------------------------------------------------------------------
  #
  #                       SCHEMAS
  #
  #--------------------------------------------------------------------
  schemas:
    daemonGridConfig:
      title: Daemon Grid Configuration
      description: Provides the grid-configuration information to a server or backup
        manager along with a verification hash.
      type: object
      properties:
        configuration:
          $ref: "#/components/schemas/gridConfig"
        hash:
          description: The SHA-256 hash of the grid configuration.
          type: string
          example: "3a7bd3e2360a3d80f8b1d1c1d6f9c4e5b3c6e0f1e2d3c4b5a6b7c8d9e0f1a2b"
    daemonHostInfo:
      title: Daemon Host Information
      description: Provides information about a host that is being added to the grid.
      type: object
      properties:
        name:
          description: The name of the host.
          type: string
          example: "host1.mycompany.com"
        aliases:
          description: An array of aliases for the host.
          type: array
          items:
            type: string
          example: ["host1", "h1"]
        ipAddresses:
          description: An array of IP addresses for the host.
          type: array
          items:
            type: string
          example: ["192.168.1.1", "192.168.1.2"]
        k8sPodName:
          description: The name of the Kubernetes pod in which the daemon is running.
          type: string
          example: "sas-workload-orchestrator-daemon-abc123"
        k8sNamespace:
          description: The Kubernetes namespace in which the daemon pod is running.
          type: string
          example: "sas-namespace"
        k8sIngressURL:
          description: The ingress URL through which the daemon can be reached.
          type: string
          example: "http://daemon.mycompany.com"
        k8sInternalTLS:
          description: A flag that indicates whether the daemon uses TLS for internal
            communication.
          type: boolean
          example: true
        staticInfo:
          description: Static information about the host.
          type: object
          properties:
            version:
              description: The version of this representation.
              type: integer
              format: int32
              readOnly: true
              minimum: 1
              maximum: 1
            cpuRating:
              description: The CPU rating of the host.
              type: number
              format: double
              example: 2000.125
            cores:
              description: The number of CPU cores available on the host.
              type: number
              format: double
              example: 4.125
            memory:
              description: The amount of memory available on the host (in MB).
              type: number
              format: double
              example: 8192.125
            swap:
              description: The amount of swap space available on the host (in MB).
              type: number
              format: double
              example: 2048.125
            temp:
              description: The amount of temporary storage available on the host (in
                MB).
              type: number
              format: double
              example: 10240.125
            startTime:
              description: The time when the daemon started on the host.
              type: string
              format: date-time
              example: "2023-10-01T12:00:00Z"
            buildDate:
              description: The build date of the daemon software.
              type: string
              example: "2023-09-15T08:00:00Z"
            osName:
              description: The name of the operating system on the host.
              type: string
              example: "Linux"
            osDesc:
              description: A description of the operating system on the host.
              type: string
              example: "Ubuntu 20.04.1 LTS"
            cpuVendor:
              description: The CPU vendor of the host.
              type: string
              example: "Intel"
            cpuArch:
              description: The CPU architecture of the host.
              type: string
              example: "x86_64"
            podMemory:
              description: The amount of memory allocated to the daemon's Kubernetes
                pod (in MB).
              type: number
              format: double
              example: 2048.125
            podCores:
              description: The number of CPU cores allocated to the daemon's Kubernetes
                pod.
              type: number
              format: double
              example: 1.125
            properties:
              description: An array of Kubernetesnode labels for the host.
              type: array
              items:
                type: string
              example: ["gpuEnabled=true", "ssdStorage=false"]
            allocatables:
              description: The current allocatable resources on the host.
              type: object
              example:
                nvidia.gpu: 10.125
                bigdata.sas.com/spark: 5
        version:
          description: Specifies the version number of this representation schema.
            This is version 3.
          type: integer
          format: int32
          readOnly: true
          minimum: 1
          maximum: 4
    daemonHostState:
      title: Daemon Host State
      description: Provides the state information about a host that is being updated
        in the grid.
      type: object
      properties:
        name:
          description: The name of the host.
          type: string
          example: "host1.mycompany.com"
        dynamicInfo:
          description: Dynamic information about the host.
          type: object
          properties:
            version:
              description: Specifies the version number of this representation schema.
                This is version 4.
              type: integer
              format: int32
              readOnly: true
              minimum: 1
              maximum: 4
            utilization:
              description: The current CPU utilization of the host (as a percentage).
              type: number
              format: double
              example: 75.5
            runQueue15s:
              description: The 15-second run queue length of the host.
              type: number
              format: double
              example: 2.5
            runQueue1m:
              description: The 1-minute run queue length of the host.
              type: number
              format: double
              example: 3.125
            runQueue15m:
              description: The 15-minute run queue length of the host.
              type: number
              format: double
              example: 4.125
            diskIORate:
              description: The current disk I/O rate of the host (in KB/s).
              type: number
              format: double
              example: 1500.125
            netIORate:
              description: The current network I/O rate of the host (in KB/s).
              type: number
              format: double
              example: 3000.125
            ioRate:
              description: The current total I/O rate of the host (in KB/s).
              type: number
              format: double
              example: 4500.125
            pgRate:
              description: The current page rate of the host (in pages/s).
              type: number
              format: double
              example: 20.125
            usedMemory:
              description: The amount of memory currently used on the host (in MB).
              type: number
              format: double
              example: 4096.125
            usedSwap:
              description: The amount of swap space currently used on the host (in
                MB).
              type: number
              format: double
              example: 1024.125
            usedTemp:
              description: The amount of temporary storage currently used on the host
                (in MB).
              type: number
              format: double
              example: 5120.125
            reservedMemory:
              description: The amount of memory reserved on the host (in MB).
              type: number
              format: double
              example: 1024.125
            reservedCores:
              description: The number of CPU cores reserved on the host.
              type: number
              format: double
              example: 1.125
            jobsRunning:
              description: The number of jobs currently running on the host.
              type: integer
              format: int64
              example: 5
            jobsSuspendedAdmin:
              description: The number of jobs currently suspended by an administrator
                on the host.
              type: integer
              format: int64
              example: 1
            jobsSuspendedPreempt:
              description: The number of jobs currently suspended due to preemption
                on the host.
              type: integer
              format: int64
              example: 0
            jobsSuspendedThreshold:
              description: The number of jobs currently suspended due to threshold
                limits on the host.
              type: integer
              format: int64
              example: 0
            licenseValid:
              description: A flag that indicates whether the host's license is valid.
              type: boolean
              example: true
            licenseType:
              description: The type of license on the host.
              type: integer
              example: 1
            licenseExpireDate:
              description: The expiration date of the host's license.
              type: string
              example: "2024-12-31"
            hostCordoned:
              description: A flag that indicates whether the host is cordoned (i.e.,
                not available for scheduling new jobs).
              type: boolean
              example: false
            hostUpdateTime:
              description: The time when the host last sent an update to the manager.
              type: string
              format: date-time
              example: "2023-10-01T12:30:00Z"
            hostInDiskPressure:
              description: A flag that indicates whether the host is currently experiencing
                disk pressure.
              type: boolean
              example: false
            hostInMemoryPressure:
              description: A flag that indicates whether the host is currently experiencing
                memory pressure.
              type: boolean
              example: false
            hostInPIDPressure:
              description: A flag that indicates whether the host is currently experiencing
                PID pressure.
              type: boolean
              example: false
            hostNetworkUnavailable:
              description: A flag that indicates whether the host's network is currently
                unavailable.
              type: boolean
              example: false
        version:
          description: Specifies the version number of this representation schema.
            This is version 1.
          type: integer
          format: int32
          readOnly: true
          minimum: 1
          maximum: 1
    jobInfo:
      title: Job Information
      description: Provides information about a job including its request and processing
        information.
      type: object
      properties:
        id:
          description: The ID of the job.
          type: integer
          readOnly: true
        request:
          $ref: "#/components/schemas/jobRequestInfo"
        processingInfo:
          $ref: "#/components/schemas/jobProcessingInfo"
        version:
          description: Specifies the version number of this representation schema.
            This is version 1.
          type: integer
          format: int32
          readOnly: true
          minimum: 1
          maximum: 1
    loggersLevelInfo:
      title: Host Logger Information
      description: Information about the loggers and their assigned levels for a specific
        host.
      type: object
      properties:
        hostName:
          description: The name of the host on which the loggers are located.
          type: string
          example: "myhost"
        loggers:
          description: An array of logger-level objects.
          type: array
          items:
            $ref: "#/components/schemas/loggerLevelInfo"
        version:
          description: Specifies the version number of this representation schema.
            This is version 1.
          type: integer
          format: int32
          readOnly: true
          minimum: 1
          maximum: 1
    logsCollection:
      title: Log Line Object Collection
      description: Provides an array of log lines (in the SAS collection format) that
        meet the requested filters.
      type: object
      allOf:
      - $ref: "#/components/schemas/baseCollection2"
      - type: object
        required:
        - items
        properties:
          items:
            description: An array of log line objects.
            type: array
            items:
              $ref: "#/components/schemas/logLine"
    logsInfo:
      title: Log Line Object Array
      description: Provides an array of log lines (in the original SAS Workload Orchestrator
        format) that meet the requested filters.
      type: object
      properties:
        jobs:
          description: An array of log line objects.
          type: array
          items:
            $ref: "#/components/schemas/logLine"
        version:
          description: Specifies the version number of this representation schema.
            This is version 1.
          type: integer
          format: int32
          readOnly: true
          minimum: 1
          maximum: 1
    tenantConfig:
      title: Tenant Configuration
      description: Provides the configuration of users in the grid configuration for
        a specific tenant.
      type: object
      properties:
        tenant:
          description: The name of the tenant.
          type: string
          example: "acme"
        admins:
          description: An array of tenant administrator user names or group names.
          type: array
          items:
            type: string
          example: ["tenantAdmin1", "tenantAdmin2"]
        queues:
          description: The queues configured for the tenant.
          type: array
          items:
            $ref: "#/components/schemas/tenantQueueConfig"
        userGroups:
          description: The user groups configured for the tenant.
          type: array
          items:
            $ref: "#/components/schemas/userGroupConfig"
        version:
          description: Specifies the version number of this representation schema.
            This is version 1.
          type: integer
          format: int32
          minimum: 1
          maximum: 1
    loggersLevelRequest:
      title: Hosts' Loggers' Level Set Request
      description: Loggers and their requested levels to set on specific hosts.
      type: object
      properties:
        host:
          description: An array of host names to set the requested logger level.
          type: array
          items:
            type: string
          example: ["myhost1", "myhost2"]
        loggers:
          description: An array of logger-level objects.
          type: array
          items:
            $ref: "#/components/schemas/loggerLevelInfo"
        version:
          description: Specifies the version number of this representation schema.
            This is version 1.
          type: integer
          format: int32
          readOnly: true
          minimum: 1
          maximum: 1
    gridConfig:
      title: Grid Configuration Information
      description: Provides the grid-configuration information.
      type: object
      properties:
        name:
          description: The name of the grid configuration.
          type: string
          example: "Grid Config as of 2022-10-13"
        description:
          description: The description of the grid configuration.
          type: string
          example: "Grid Config that adds time-based overrides for the weekend."
        jobChangeCmd:
          description: >-
            Points to a script file that is called when a job is submitted.
            The script file is passed the job request JSON on STDIN and must return
            the modified job request JSON on STDOUT.
            If the return code of the script is non-zero, the job is rejected.
            The path must be mounted in the sas-workload-orchestrator statefulset.
          type: string
          example: "/scripts/jobChangeCmd.sh"
        port:
          description: The port on which the SGMG executable listens. This cannot
            be changed.
          type: integer
          format: int32
          readOnly: true
        userNamesNoCase:
          description: When set, a flag that indicates user-name comparisons are case
            insensitive.
          type: boolean
        hostInactiveSecs:
          description: The number of seconds that the manager waits to hear from a
            host before it is considered inactive.
          type: integer
          example: 60
          minimum: 60
          maximum: 3600
          default: 60
        hostUnknownSecs:
          description: The number of seconds that a host remains in the INACTIVE state
            before it is purged from the grid.
          type: integer
          example: 900
          minimum: 300
          maximum: 21600
          default: 900
        hostUpdateSecs:
          description: The number of seconds that a daemon waits between sending host
            updates to the manager.
          type: integer
          example: 15
          minimum: 5
          maximum: 300
          default: 15
        jobPurgeSecs:
          description: The number of seconds that a completed job remains in memory
            before it is purged from the manager.
          type: integer
          example: 3600
          minimum: 15
          maximum: 604800
          default: 3600
        jobUpdateSecs:
          description: >-
            The number of seconds that a daemon waits between updating the manager
            with job status information.
            Job state change events can cause the daemon to update the state sooner.
          type: integer
          example: 15
          minimum: 5
          maximum: 300
          default: 15
        jobLifetimeCount:
          description: >-
            The maximum number of completed job records allowed to remain in the database
            tables.
            When the number of completed job records exceeds this value, the oldest
            records are deleted.
            This field is new in version 3 of the grid configuration.
          type: integer
          example: 60000
          minimum: 1000
          maximum: 1000000
          default: 100000
        jobLifetimeDays:
          description: >-
            The number of days a completed job will remain in the database tables
            before being
            automatically deleted from the tables.
            This field is new in version 3 of the grid configuration.
          type: integer
          example: 90
          minimum: 7
          maximum: 100000
          default: 60
        managerUpdateSecs:
          description: >-
            The maximum number of seconds that a manager waits between updates of
            the manager state.
            Other events (job updates, host updates) can cause the manager to update
            the state sooner.
          type: integer
          example: 15
          minimum: 5
          maximum: 300
          default: 15
        roundRobinSecs:
          description: The number of seconds that a host must not be used if the host
            was just scheduled a job. Zero means disabled.
          type: integer
          example: 1
          minimum: 0
          maximum: 300
          default: 0
        schedulerUpdateSecs:
          description: >-
            The maximum number of seconds that a manager waits between trying to schedule
            pending jobs.
            Job events (job completion, job submission) can cause the manager to run
            the scheduler sooner.
          type: integer
          example: 15
          minimum: 5
          maximum: 300
          default: 15
        scalingRequestSecs:
          description: The number of seconds that the host manager delays between
            receiving a scaling request from the scheduler and creating a scaling
            pod. Used only when scaled hosts exist and waiting might result in another
            job completing, which frees up an opportunity for a job to run. Ignored
            when scaled hosts do not exist because there is never an opportunity for
            a job to run when hosts do not exist.
          type: integer
          example: 60
          minimum: 0
          maximum: 3600
          default: 0
        scalingHostWaitSecs:
          description: The number of seconds that the host manager waits between the
            creation of a scaling pod and a response from the new host before cancelling
            the scaling request.
          type: integer
          example: 60
          minimum: 60
          maximum: 3600
          default: 600
        scalingHostUpSecs:
          description: The number of seconds that the host manager waits after a new
            scaling host responds before honoring a new scaling request for the same
            host type.
          type: integer
          example: 300
          minimum: 0
          maximum: 3600
          default: 300
        maxStdOutLines:
          description: The number of lines to keep at the end of the STDOUT/STDERR
            of the job. If set to 0, all lines are kept.
          type: integer
          example: 50
          minimum: 0
          maximum: 10000
          default: 50
        userResources:
          description: An array of user-defined resources.
          type: array
          items:
            $ref: "#/components/schemas/userDefinedResource"
        compareOrder:
          description: |-
            An array of predefined metrics that determines the compare order for host selection.
            The order in which the metric is listed is the order that the metric is considered in the compare.
            The metric can be any resource name, but generally is a predefined dynamic-resource name or
            any user-defined host-dynamic resource name. The predefined resource names are:
            |  **Field Name**  |        **Description**               | **Type** |
            |------------------|--------------------------------------|----------|
            |cpuRating         | The CPU rating of the host.          | static   |
            |cores             | The number of cores on the host.     | static   |
            |memory            | The total memory on the host.        | static   |
            |swap              | The total swap space on the host.    | static   |
            |temp              | The total temp space on the host.    | static   |
            |utilization       | The total utilization on the host.   | dynamic  |
            |runQueue15s       | The 15-second run queue on the host. | dynamic  |
            |runQueue1m        | The 1-minute run queue on the host.  | dynamic  |
            |runQueue15m       | The 15-minute run queue on the host. | dynamic  |
            |diskIORate        | The disk IO rate on the host.        | dynamic  |
            |netIORate         | The network IO rate on the host.     | dynamic  |
            |ioRate            | The total IO rate on the host.       | dynamic  |
            |pgRate            | The page rate on the host.           | dynamic  |
            |usedMemory        | The used memory on the host.         | dynamic  |
            |usedSwap          | The used swap space on the host.     | dynamic  |
            |usedTemp          | The used temp space on the host.     | dynamic  |
          type: array
          items:
            type: string
          example: ["runQueue15s", "ioRate"]
        admins:
          description: An array of administrator user names or group names.
          type: array
          items:
            type: string
          example: ["admin1", "admin2"]
        tenantAdmins:
          description: An array of tenant administrators.
          type: array
          items:
            $ref: "#/components/schemas/tenantAdmin"
        hostTypes:
          description: An array of host type configurations.
          type: array
          items:
            $ref: "#/components/schemas/hostType"
        queues:
          description: An array of queue configurations.
          type: array
          items:
            $ref: "#/components/schemas/queueConfig"
        userGroups:
          description: An array of user-group configurations.
          type: array
          items:
            $ref: "#/components/schemas/userGroupConfig"
        hostGroups:
          description: An array of host group configurations.
          type: array
          items:
            $ref: "#/components/schemas/hostGroupConfig"
        version:
          description: Specifies the version number of this representation schema.
            This is version 3.
          type: integer
          format: int32
          readOnly: true
          minimum: 1
          maximum: 3
    api:
      title: API
      description: The list of links to top-level resources and operations available
        from the root of the API.
      x-sas-media-type:
        name: application/vnd.sas.api
        version: 1
      required:
      - version
      - links
      properties:
        version:
          type: integer
          description: The version number of the API representation. This is version
            1.
          minimum: 1
        links:
          description: The API's top-level links.
          type: array
          items:
            $ref: "#/components/schemas/link"
    error2:
      title: Error
      type: object
      description: The representation of an error.
      x-sas-media-type:
        name: application/vnd.sas.error
        version: 2
      required:
      - httpStatusCode
      - version
      properties:
        message:
          type: string
          description: The message for the error.
        id:
          type: string
          description: The string ID for the error.
        errorCode:
          type: integer
          description: The numeric ID for the error.
        httpStatusCode:
          type: integer
          description: The HTTP status code for the error.
        details:
          type: array
          description: Messages that provide additional details about the cause of
            the error.
          items:
            type: string
        remediation:
          type: string
          description: A message that describes how to resolve the error.
        errors:
          type: array
          description: Any additional errors that occurred.
          items:
            $ref: "#/components/schemas/error2"
        links:
          type: array
          description: The links that apply to the error.
          items:
            $ref: "#/components/schemas/link"
        version:
          type: integer
          description: The version number of the error representation. This representation
            is version 2.
    tenantAdmin:
      title: Tenant Administrator
      description: Provides the list of tenant user names or groups that can be a
        tenant for a specific group.
      type: object
      properties:
        tenant:
          description: The name of tenant.
          type: string
          example: "acme"
        admins:
          description: An array of tenant administrator user names or group names.
          type: array
          items:
            type: string
          example: ["tenantAdmin1", "tenantAdmin2"]
    jobProcessingInfo:
      title: Job Processing Information
      description: Provides information about a job's processing.
      type: object
      readOnly: true
      properties:
        state:
          description: |-
            The state of the job. The following states are available:
            | **State Name**                | **Description** |
            |-------------------------------|-----------------|
            | UNKNOWN                       | Job state cannot be determined. |
            | PENDING                       | Job is pending in a queue. |
            | PENDING-SUSPENDED-ADMIN       | Job is pending in the queue, but suspended by the administrator. |
            | STARTING                      | Job has been sent to a host to run. |
            | STARTING-SUSPENDED-ADMIN      | Job is at the execution host, but was suspended by the administrator before the host was able to launch the command. |
            | STARTING-SUSPENDED-THRESHOLD  | Job is at the execution host, but was suspended by the host's suspension threshold before the host was able to launch the command. |
            | STARTING-SUSPENDED-PREEMPTION | Job is at the execution host, but was suspended by the scheduler because the job was preempted before the host was able to launch the command. |
            | RUNNING                       | Job is running on the execution host. |
            | RUNNING-SUSPENDED-ADMIN       | Job was running at the execution host, but was suspended by the administrator. |
            | RUNNING-SUSPENDED-THRESHOLD   | Job was running at the execution host, but was suspended by the host's suspension threshold. |
            | RUNNING-SUSPENDED-PREEMPTION  | Job was running at the execution host, but was suspended by the scheduler because the job was preempted. |
            | LAUNCH-ERROR                  | Job failed to launch correctly. |
            | COMPLETED                     | Job ran and completed with a zero return code. |
            | FAILED                        | Job ran and completed with a non-zero return code. |
            | KILLED                        | Job was killed through the REST API. |
            | KILLED-LIMIT                  | Job was killed because it exceeded a limit specified in the job or queue. |
            | HOST-FAILED                   | Job was killed because its execution host failed and the job was not marked as being re-runnable. |
            | REQUEUING                     | Job is in the process of being requeued to be restarted. |
          type: string
          maxLength: 40
          enum:
          - UNKNOWN
          - PENDING
          - PENDING-SUSPENDED-ADMIN
          - STARTING
          - STARTING-SUSPENDED-ADMIN
          - STARTING-SUSPENDED-THRESHOLD
          - STARTING-SUSPENDED-PREEMPTION
          - RUNNING
          - RUNNING-SUSPENDED-ADMIN
          - RUNNING-SUSPENDED-THRESHOLD
          - RUNNING-SUSPENDED-PREEMPTION
          - LAUNCH-ERROR
          - COMPLETED
          - FAILED
          - KILLED
          - KILLED-LIMIT
          - HOST-FAILED
          - REQUEUING
          example: COMPLETED
        pendingCause:
          description: |-
            The reason the job is still pending. The following causes are available:
            | **Cause**                            | **Description** |
            |--------------------------------------|-----------------|
            | PEND-NOT-RUN-THROUGH-SCHEDULER       | The job has not been run through the scheduler. |
            | PEND-NOT-ENOUGH-GLOBAL-CON-RES       | The job has a consumable-resource requirement that cannot be met. |
            | PEND-NOT-ENOUGH-GLOBAL-REQ-RES       | The job has a resource requirement that cannot be met. |
            | PEND-NO-HOSTS                        | There are no hosts available for this job. |
            | PEND-HOSTS-FULL-NO-QUEUES-TO-PREEMPT | The hosts available to this job are all full and there are no queues that can be preempted. |
            | PEND-HOSTS-FULL-NO-JOB-TO-PREEMPT    | The hosts available to this job are all full and there are no jobs that can be preempted. |
            | PEND-QUEUE-FULL-JOBS                 | The queue has reached the limit of jobs per queue. |
            | PEND-QUEUE-FULL-USER                 | The queue has reached the limit of jobs per user for this user. |
            | PEND-QUEUE-INACTIVE                  | The job's queue is inactive. |
            | PEND-CANNOT-MERGE-QUEUE-INFO         | The queue information could not be merged. |
            | PEND-NOT-AFTER-STARTTIME             | The job has a start time which is in the future. |
            | PEND-BACKOFF-DELAY                   | The job is waiting for a backoff delay to expire. |
            | PEND-FAILED-TO-START                 | The job failed to start on the assigned host. |
          type: string
          maxLength: 40
          example: "PEND-NO-HOSTS"
        hostSchedulingInfo:
          description: Scheduling information for all hosts selected as candidates
            to run this job.
          type: array
          items:
            $ref: "#/components/schemas/jobHostSchedulingInfo"
        queue:
          description: Queue on which the job is executing.
          type: string
          maxLength: 250
          example: "normal"
        exchangeCreds:
          description: A flag that is the logical OR of the exchangeCreds flag on
            the job and the exchangeCreds flag on the queue. Valid in SAS v9 Windows
            only.
          type: boolean
          example: false
          deprecated: true
        userGroup:
          description: A user group that the group was associated with when it was
            submitted. Used to determine if the user-group administrator can manage
            this job.
          type: string
          maxLength: 100
          example: "accounting"
        suspendFlags:
          description: Flags that are set to determine the various states of suspension.
            The flag is used internally to determine when a job is suspended and the
            methods of the suspension.
          type: integer
          example: 256
        queuePriority:
          description: The priority of the queue at the time the job started running.
          type: integer
          example: 34
        submitTime:
          description: The time the job was submitted.
          type: string
          format: date-time
          example: "2022-07-28T12:03:14Z"
        startTime:
          description: The time the job started running.
          type: string
          format: date-time
          example: "2022-07-28T12:03:14Z"
        endTime:
          description: The time the job completed processing.
          type: string
          format: date-time
          example: "2022-07-28T12:04:10Z"
        lastCheckTime:
          description: The time the processing information was last checked, updated,
            or both.
          type: string
          format: date-time
          example: "2022-07-28T12:04:14Z"
        launchStatus:
          description: The error code from the launch of the process. If non-zero,
            the launch failed.
          type: integer
          example: 0
        processId:
          description: The process ID of the job process. Valid in SAS v9 only.
          type: integer
          example: 34887
        k8sObjName:
          description: The Kubernetes pod name executing the job.
          type: string
          example: "sas-batch-server-ad54-12347896-23"
        requeueCount:
          description: The number of times a restartable job has been requeued.
          type: integer
          example: 0
        executionHost:
          description: The host name of the host on which the job is executing.
          type: string
          example: "host1.mycompany.com"
        executionIP:
          description: The IP address of the execution host.
          type: string
          maxLength: 100
          example: "10.20.3.45"
        executionHostAliases:
          description: >-
            An array of aliases of the execution host. On the SAS Viya platform this
            usually includes the sas-workload-orchestrator daemon
            pod that is managing the job on the assigned node.
          type: array
          items:
            type: string
          example: ["sas-workload orchestrator-da463"]
        exitCode:
          description: The return code from the process. If the code is non-zero,
            the job is assumed to have failed.
          type: integer
          example: 102
        totalTimePending:
          description: The total number of seconds that the job spent pending.
          type: integer
          format: uint64
          example: 5
        totalTimeStarting:
          description: The total number of seconds that the job spent starting.
          type: integer
          format: uint64
          example: 32
        totalTimeRunning:
          description: The total number of seconds that the job spent running.
          type: integer
          format: uint64
          example: 640
        totalTimeSuspAdmin:
          description: The total number of seconds that the job spent suspended due
            to a REST API request.
          type: integer
          format: uint64
          example: 0
        totalTimeSuspThresh:
          description: The total number of seconds that the job spent suspended due
            to exceeding a host suspension threshold.
          type: integer
          format: uint64
          example: 0
        totalTimeSuspPreempt:
          description: The total number of seconds that the job spent suspended due
            to preemption. Valid in SAS v9 only.
          type: integer
          format: uint64
          example: 0
        hosts:
          description: >-
            The intersected list of host names from the job request and the queue
            that was used to determine where this job was allowed to run.
          type: array
          items:
            type: string
          example: ["host1.mycompany.com", "host2.mycompany.com"]
        requiredTags:
          description: The combined list of tags from the job request and queue that
            was used to determine where this job was allowed to run.
          type: array
          items:
            type: string
          example: ["gpuEnabled", "fastIO"]
        consumedResources:
          description: >-
            The combined array of consumable static-resource values from the job request
            and from the queue
            that was used to determine where this job was allowed to run.
          type: array
          items:
            $ref: "#/components/schemas/jobStaticResourceRequirement"
        requiredResources:
          description: >-
            The combined array of required static-resource values from the job request
            and from the queue
            that was used to determine where this job was allowed to run.
          type: array
          items:
            $ref: "#/components/schemas/jobStaticResourceRequirement"
        limits:
          description: >-
            The combined array of limit values from the job request and from the queue
            that is used to limit the job's processing.
          type: array
          items:
            $ref: "#/components/schemas/jobResourceLimit"
        limitValues:
          description: The actual limit values as reported during processing.
          type: array
          items:
            $ref: "#/components/schemas/jobResourceLimit"
        stdOutLines:
          description: The STDOUT lines captured during processing. The number of
            lines is determined by the 'maxStdOutLines' value in the grid configuration.
          type: array
          items:
            type: string
          example: ["This was output by the running process to STDOUT", "So was this"]
    queueConfig:
      title: Queue Configuration
      type: object
      description: Provides the queue configuration that is used to implement workload-management
        policy.
      allOf:
      - $ref: "#/components/schemas/queueTimeBasedParameters"
      - type: object
        properties:
          name:
            description: The name of the queue.
            type: string
            maxLength: 250
            example: Priority
          description:
            description: The description of the queue.
            type: string
            maxLength: 1000
            example: "Queue for high priority jobs"
          tenant:
            description: The tenant associated with this queue. If this is not provided,
              it is assumed that the queue is for the default provider.
            type: string
            maxLength: 250
            example: "acme"
          timeBasedOverrides:
            description: An array of time-based parameter overrides along with the
              time the overrides take affect.
            type: array
            items:
              $ref: "#/components/schemas/queueTimeBasedOverride"
          version:
            description: Specifies the version number of this representation schema.
              This is version 1.
            type: integer
            format: int32
            readOnly: true
            minimum: 1
            maximum: 2
    tenantQueueConfig:
      title: Tenant Queue Configuration
      description: Provides the queue configuration that can be modified by a tenant
        administrator.
      type: object
      allOf:
      - $ref: "#/components/schemas/tenantQueueTimeBasedInfo"
      - type: object
        properties:
          name:
            description: The name of the queue.
            type: string
            maxLength: 250
            example: Priority
          description:
            description: The description of the queue.
            type: string
            maxLength: 1000
            example: "Queue for high priority jobs"
          tenant:
            description: The name of the tenant.
            type: string
            maxLength: 250
            example: "acme"
          timeBasedOverrides:
            description: Time-based overrides for the queue administrators and users.
            type: array
            items:
              $ref: "#/components/schemas/tenantQueueTimeBasedConfig"
    loggerLevelInfo:
      title: Logger Information
      description: Information about a logger and its assigned level.
      type: object
      properties:
        name:
          description: The name of the logger.
          type: string
          example: App.Grid.SGMG.Log.HTTP
        level:
          description: |-
            The logging level that is currently assigned to the logger. The following levels are available:
            | **Level Name** | **Description** |
            |----------------|-----------------|
            | NONE  | Turn off all logging from this logger. |
            | FATAL | Only log FATAL messages. |
            | ERROR | Only log FATAL and ERROR messages. |
            | WARN  | Only log FATAL, ERROR, and WARN messages. |
            | INFO  | Only log FATAL, ERROR, WARN, and INFO messages. |
            | DEBUG | Only log FATAL, ERROR, WARN, INFO, and DEBUG messages. |
            | TRACE | Only log FATAL, ERROR, WARN, INFO, DEBUG, and TRACE messages. |
            | ALL   | Log all messages. |
            | NULL  | Log messages based on the parent-logger setting. |
          type: string
          enum:
          - NONE
          - FATAL
          - ERROR
          - WARN
          - INFO
          - DEBUG
          - TRACE
          - ALL
          - "NULL"

    baseCollection2:
      title: Base Collection
      description: This is a base schema used to define paginated collections of resources.
        This base schema is extended by other schemas in APIs by adding an 'items'
        array property. These extensions define the application/vnd.sas.collection
        media type (version 2)
      properties:
        name:
          type: string
          description: The name of the collection.
        start:
          type: integer
          description: The zero-based index of the first item in the collection.
          format: int64
        limit:
          type: integer
          description: The number of items that were requested for the collection.
        count:
          type: integer
          description: If populated indicates the number of items in the collection.
          format: int64
        accept:
          type: string
          description: A space-delimited list of media types from which an `Accept`
            header may be constructed.
        links:
          type: array
          description: The links that apply to the collection.
          items:
            $ref: "#/components/schemas/link"
        version:
          type: integer
          description: The version number of the collection representation. This representation
            is version 2.
    userDefinedResource:
      title: User Defined Resource
      description: Provides information about a user-defined resource.
      type: object
      properties:
        name:
          description: The name of the user-defined resource.
          type: string
          example: "SASWORK Size"
        type:
          description: |-
            The type of user-defined resource. The following types are available:
            | **Type Name**          | **Description** |
            |------------------------|-----------------|
            | globalStatic           | (DEPRECATED) A value that is static in the cluster. The value can be compared against a global value by jobs as they are submitted. The resource value is initialized on startup and when the configuration changes. |
            | globalStaticConsumable | A value that is static in the cluster. The value can be consumed by jobs as they are submitted. The resource value is initialized on startup and when the configuration changes. |
            | hostStatic             | A value that is static to a host. When jobs are submitted, they compare this value against a host's value. The resource value is initialized on startup and when the manager makes changes. |
            | hostStaticConsumable   | A value that is static to a host. The value can be consumed by jobs as they are submitted. The resource value is initialized on startup and when the manager makes changes. |
            | hostDynamic            | A value that is dynamic to a host. The value can be used in the compare order for scheduling jobs, schedule thresholds, and suspension thresholds. The value is updated periodically by the status manager when it updates the predefined-dynamic values. |
          type: string
          enum:
          - globalStatic
          - globalStaticConsumable
          - hostStatic
          - hostStaticConsumable
          - hostDynamic
        greaterIsBetter:
          description: >-
            A flag that indicates a larger value is better than a smaller value in
            comparisons.
            When this resource is used in the compare order, the flag tells us how
            to use the value in an evaluation.
          type: boolean
        scriptFilePath:
          description: >-
            The path to the script file that is called to produce the resource's value.
            The script is passed the name of the resource as a parameter. The script
            outputs
            a double value to STDOUT that represents the current value of the resource.
            The path to the script file must be a mounted path in the sas-workload-orchestrator
            statefulset and daemonset.
          type: string
        value:
          description: If this is a static resource and scriptFilePath is not specified,
            a hard-coded value can be used.
          type: number
          example: 34.73
    hostType:
      title: Host Type Configuration
      description: >-
        Provides the configuration of a host type that can be used in queue definitions
        and scheduling processing.
      type: object
      allOf:
      - $ref: "#/components/schemas/hostTypeTimeBasedParameters"
      - type: object
        properties:
          name:
            description: The name of the host type.
            type: string
            maxLength: 250
            example: "Overflow Hosts"
          description:
            description: The description of the host type.
            type: string
            example: "This host type will be used for any overflow work needed."
          role:
            description: >-
              The host role for which this host type is used. One of 'manager' or
              'server'.
              The SAS Viya platform allows defining host types for the 'server' role
              only.
            type: string
            enum:
            - "manager"
            - "server"
          scalable:
            description: A flag that indicates this host type is scalable in Kubernetes.
            type: boolean
          externalURL:
            description: >-
              A URL to redirect REST API calls for the manager.
              This is not used on the SAS Viya platform.
            type: string
            example: "https://my.loadbalancer.mycompany.com:1234"
          hostNames:
            description: An array of host names that are in this host type.
            type: array
            items:
              type: string
            example: ["host1.mycompany.com", "host2.mycompany.com"]
          hostNamePatterns:
            description: >-
              An array of host name patterns to compare against a host name for a
              match. The comparison determines if the host can be in this host type.
              The pattern must be an International Components for Unicode (ICU) regular
              expression to match against any daemon hosts' short host name or its
              aliases.
            type: array
            items:
              type: string
            example: ["host*.mycompany.com", "myhost*.mycompany.com"]
          hostIpPatterns:
            description: >-
              An array of IP address patterns to compare against a host's IP address
              for a match. The comparison determines if the host can be in this host
              type.
              The pattern must be an International Components for Unicode (ICU) regular
              expression to match against the IP addresses specified by the host or
              associated with the connection socket on the manager.
            type: array
            items:
              type: string
            example: ["10.2.34.*", "10.2.35.*"]
          hostIpRanges:
            description: >-
              An array of IP address-range patterns to compare against a host's IP
              address for a match. The comparison determines if the host can be in
              this host type.
              The pattern must be a CIDR-range expression to determine if the connecting
              host is in the specified subnet or matches the specified address.
              Standard IPV4 or IPV6 addresses can be specified or CIDR notation of
              subnets as defined in Wikipedia's Classless Inter-Domain Routing.
            type: array
            items:
              type: string
            example: ["10.2.34.0/16", "10.2.35.0/16"]
          hostProperties:
            description: >-
              An array of key/value strings of the form '<key>=<value>' to compare
              against a host's IP address for a match. The comparison determines if
              the host can be in this host type.
              On the SAS Viya platform the key/value strings come from the node labels.
            type: array
            example: ["gpuEnabled=true"]
          timeBasedOverrides:
            description: An array of time-based parameter overrides along with the
              time the overrides take affect.
            type: array
            items:
              $ref: "#/components/schemas/hostTypeTimeBasedOverride"
          version:
            description: Specifies the version number of this representation schema.
              This is version 1.
            type: integer
            format: int32
            readOnly: true
            minimum: 1
            maximum: 2
    jobRequestInfo:
      title: Job Request Information
      description: Provides information about a job request.
      type: object
      properties:
        name:
          description: The name of the job.
          type: string
          maxLength: 100
          example: "End of month processing"
        queue:
          description: The name of the queue to which the job is submitted.
          type: string
          maxLength: 250
          example: "priority"
        project:
          description: The project to associate with the job.
          type: string
          maxLength: 100
          example: "Accounting"
        user:
          description: The user that submitted the job.
          type: string
          maxLength: 100
          example: "user132"
        tenant:
          description: The tenant of the user.
          type: string
          maxLength: 100
          example: "acme"
        startAfter:
          description: The date-time to wait before releasing the job to scheduling.
          type: string
          format: date-time
          example: "2022-07-28T12:03:14Z"
        restartable:
          description: A flag that indicates the job is restartable.
          type: boolean
        exchangeCreds:
          description: A flag that indicates the Kerberos credentials must be exchanged.
            Only used on Windows. This is not used on the SAS Viya platform.
          type: boolean
          deprecated: true
        type:
          description: |-
            The job type.
            * COMMAND jobs use the cmd, args, and envVars fields. COMMAND jobs are launched by TKELS on an execution host. If the field is not provided, COMMAND is assumed.
            * KUBERNETESJOB uses the k8sJobDef, k8sNamespace, and k8sSecrets fields. KUBERNETESJOBs are launched as pods in Kubernetes.
          type: string
          maxLength: 40
          enum:
          - COMMAND
          - KUBERNETESJOB
          example: "COMMAND"
        cmd:
          description: The command to execute the job for a COMMAND job type.
          type: string
          example: "/bin/bash"
        args:
          description: The name of the job.
          type: array
          items:
            type: string
          example: ["-c", "ls", "-al"]
        envVars:
          description: The environment variables to set for the process.
          type: array
          items:
            $ref: "#/components/schemas/jobEnvVar"
        k8sJobDef:
          description: A Kubernetes PodSpec, PodTemplate, or Job definition in JSON
            for the pod to be launched.
          type: string
        k8sNamespace:
          description: >-
            The namespace in which the job must start. The namespace must be set to
            the current namespace.
            SAS Workload Orchestrator does not support launching pods in a namespace
            other than the current namespace.
          type: string
          example: "my-namespace"
        k8sSecrets:
          description: The list of environment variables in the k8sJobDef that must
            be presented as secrets instead of environment variables.
          type: array
          items:
            type: string
          example: ["MY_SPECIAL_TOKEN"]
        hosts:
          description: |-
            The list of host names on which this job is allowed to run. When a job is submitted, SAS Workload Orchestrator determines the hosts
            on which a job can run by using the following criteria:
            * If both the job and the queue specify hosts, only the hosts that are in both the job's host list and the queue's host list are used. If no hosts are in both lists, the job does not run.
            * If the job request specifies hosts, but the queue does not, the job's host list is used.
            * If the job request does not specify hosts, but the queue does, the queue's host list is used.
            * If neither the job nor the queue specify hosts, all hosts are used.
          type: array
          items:
            type: string
          example: ["host1.mycompany.com", "host2.mycompany.com"]
        requiredTags:
          description: >-
            The list of tags required to be on hosts for this job to run. The union
            of the requiredTags and the queue's requiredTags
            determine the final list of requiredTags.
          type: array
          items:
            type: string
          example: ["gpuEnabled", "fastIO"]
        consumedResources:
          description: >-
            An array of consumable static-resources values that must be available
            on a host before a job is scheduled to that host.
            After the job is scheduled, the resource values are decremented from the
            host's total.
            The maximum value between the job request value and the queue value is
            used to determine if the job can be scheduled.
          type: array
          items:
            $ref: "#/components/schemas/jobStaticResourceRequirement"
        requiredResources:
          description: >-
            An array of consumable static-resources values that must be available
            on a host before a job is scheduled to that host.
            The resource values are not decremented from the host's total.
            The maximum value between the job request value and the queue value is
            used to determine if the job can be scheduled.
          type: array
          items:
            $ref: "#/components/schemas/jobStaticResourceRequirement"
        limits:
          description: >-
            An array of consumable static-resources values that must be available
            on a host before a job is scheduled to that host.
            The resource values are not decremented from the host's total.
            The minimum value between the job request value and the queue value is
            used when the job is run.
          type: array
          items:
            $ref: "#/components/schemas/jobResourceLimit"
        properties:
          description: The list of key/value pairs associated with the job.
          type: array
          items:
            type: string
          example: ["key1=value1", "key2=value2"]
    userGroupConfig:
      title: User Group Configuration
      description: Provides configuration information about a user group.
      type: object
      properties:
        name:
          description: The name of a user group. On the SAS Viya platform it is assumed
            that the user group is defined in the Identities service.
          type: string
          example: NormalQueueAdmin
        tenant:
          description: >-
            The name of the tenant in which this user group is a part. If this value
            is missing, it is assumed that the user group is part
            of the default-provider tenant.
          type: string
          example: "acme"
        members:
          description: >-
            The list of users or groups that are part of this user group.
            On the SAS Viya platform user-group membership is handled by the Identities
            service and this field is ignored.
          type: array
          items:
            type: string
          example: ["userGroupMember1", "userGroupUser2"]
        admins:
          description: A list of users or groups that are administrators of this user
            group.
          type: array
          items:
            type: string
          example: ["userGroup1Admin", "userGroupsAdmins"]
    logLine:
      title: Log Line Object
      description: Contains information about a single log line.
      type: object
      properties:
        logger:
          description: The logger that generated the line.
          type: string
          example: "App.Grid.SGMG.Log.HTTP"
        level:
          description: |-
            The logging level to which the log line was set. The following levels are available:
            | **Level Name** | **Description** |
            |----------------|-----------------|
            | FATAL | Only log FATAL messages. |
            | ERROR | Only log FATAL and ERROR messages. |
            | WARN  | Only log FATAL, ERROR, and WARN messages. |
            | INFO  | Only log FATAL, ERROR, WARN, and INFO messages. |
            | DEBUG | Only log FATAL, ERROR, WARN, INFO, and DEBUG messages. |
            | TRACE | Only log FATAL, ERROR, WARN, INFO, DEBUG, and TRACE messages. |
          type: string
          enum:
          - FATAL
          - ERROR
          - WARN
          - INFO
          - DEBUG
          - TRACE
        msg:
          description: The message text that was output.
          type: string
          example: "[00000005] sas - SAS Workload Orchestrator Daemon - Version 4.0\
            \ (2022-08-01T22:12:01Z)"
        dateTime:
          description: The GMT date and time that the message was output by the logger.
          type: string
          format: datetime
          example: "2022-08-08T02:14:28Z"
    link:
      title: Link
      type: object
      description: A link to a related operation or resource.
      required:
      - rel
      properties:
        method:
          type: string
          description: The HTTP method for the link.
        rel:
          type: string
          description: The relationship of the link to the resource.
        uri:
          type: string
          description: The relative URI for the link.
        href:
          type: string
          description: The URL for the link.
        title:
          type: string
          description: The title for the link.
        type:
          type: string
          description: The media type or link type for the link.
        itemType:
          type: string
          description: If this is a link to a container, `itemType` is the media type
            or link type for the items in the container.
        responseType:
          type: string
          description: The media type or link type of the response body for a `PUT`,
            `POST`, or `PATCH` operation.
        responseItemType:
          type: string
          description: The media type or link type of the items in the response body
            for a `PUT`, `POST`, or `PATCH` operation.
    hostGroupConfig:
      title: Host Group Configuration
      description: Provides configuration information about a host group.
      type: object
      properties:
        name:
          description: The name of host group.
          type: string
          example: FastHosts
        members:
          description: An array of host names and host groups that are members of
            this host group.
          type: array
          items:
            type: string
          example: ["host1", "slowHosts"]
    jobStaticResourceRequirement:
      title: Static Resource Requirement
      description: Provides information for specifying a host static resource required
        for a job.
      type: object
      properties:
        name:
          description: >-
            The name of the resource to check. The name can be a predefined static-resource
            name or
            the name of a user-defined globalStatic, globalStaticConsumable, hostStatic,
            or hostStaticConsumable resource.
            Predefined resource names include cpuRating, cores, memory, swap, and
            temp.
            NOTE: The globalStaticConsumable and hostStaticConsumable resources can
            be used in either the consumedResources or
            requiredResources arrays on queues and in job requests. The globalStatic
            and hostStatic resources can be used
            in only the requiredResources arrays on queues and in job requests.
          type: string
        value:
          description: The amount of resource to require, consume, or both.
          type: number
          format: double
    jobHostSchedulingInfo:
      title: Job Host Scheduling Information
      description: Provides information about a host's ability to be selected during
        the scheduling process for a specific job.
      type: object
      properties:
        name:
          description: The name of the host.
          type: string
        state:
          description: |-
            The resulting host scheduling state for a job during the last scheduling run. The following states are available:
            | **State Name**                      | **Description** |
            |-------------------------------------|-----------------|
            | HOST-STATE-AVAILABLE                | The host can run the job. |
            | HOST-STATE-FULL                     | The host has reached the maximum number of jobs it is allowed. |
            | HOST-STATE-NOT-AVAILABLE-CLOSED     | The host is closed. |
            | HOST-STATE-NOT-AVAILABLE-OVERLOADED | The host is overloaded according to a scheduling threshold. |
            | HOST-STATE-NOT-AVAILABLE-NO-SLOTS   | The host does not allow jobs to be run on it. |
            | HOST-STATE-NOT-AVAILABLE-INACTIVE   | The host is in the INACTIVE state. |
            | HOST-STATE-NOT-AVAILABLE-UNKNOWN    | The host has changed to the UNKNOWN state. |
            | HOST-STATE-NOT-IN-QUEUE             | The host is not part of the list of hosts for the queue. |
            | HOST-STATE-NOT-ENOUGH-CON-RES       | The host does not have enough of a consumable resource. |
            | HOST-STATE-NOT-ENOUGH-REQ-RES       | The host does not have the required resources. |
            | HOST-STATE-NOT-IN-FINAL-HOST-LIST   | The host is not in the list of hosts required by the job and queue. |
            | HOST-STATE-MISSING-REQUIRED-TAGS    | The host does not have all of the tags required by the job and queue. |
            | HOST-STATE-JOB-QUEUE-FULL-HOST      | The job's queue has exceeded the number of jobs it can send to this host. |
          type: string
          maxLength: 40
          enum:
          - HOST-STATE-AVAILABLE
          - HOST-STATE-FULL
          - HOST-STATE-NOT-AVAILABLE-CLOSED
          - HOST-STATE-NOT-AVAILABLE-OVERLOADED
          - HOST-STATE-NOT-AVAILABLE-NO-SLOTS
          - HOST-STATE-NOT-AVAILABLE-INACTIVE
          - HOST-STATE-NOT-AVAILABLE-UNKNOWN
          - HOST-STATE-NOT-IN-QUEUE
          - HOST-STATE-NOT-ENOUGH-CON-RES
          - HOST-STATE-NOT-ENOUGH-REQ-RES
          - HOST-STATE-NOT-IN-FINAL-HOST-LIST
          - HOST-STATE-MISSING-REQUIRED-TAGS
          - HOST-STATE-JOB-QUEUE-FULL-HOST

    tenantQueueTimeBasedInfo:
      title: Tenant Queue Time-Based Configuration
      description: Provides time-based queue configuration information that can be
        modified by a tenant administrator.
      type: object
      properties:
        admins:
          description: Tenant users and tenant groups that are the queue's administrators.
          type: array
          items:
            type: string
          example: ["tenantAdmin1", "tenantAdminGroup"]
        users:
          description: Tenant users and tenant groups that can use the queue.
          type: array
          items:
            type: string
          example: ["tenantUser1", "tenantQueueGroup"]
    jobResourceLimit:
      title: Job Resource Limit
      description: Provides information for specifying a job resource limit.
      type: object
      properties:
        name:
          description: |-
            The name of the limit resource to check. The limit names are all predefined. The following names are available:
            | **Limit Name** | **Description** |
            |--------------|-----------------|
            | maxMemory    | Maximum memory that the job can use. |
            | maxCpuTime   | Maximum CPU seconds that the job can use. |
            | maxClockTime | Maximum number of seconds that a job can run. |
            | maxIoTotal   | Maximum number of megabytes of data that the job can read and write. |
            | maxIoRate    | Maximum number of bytes that the job can read/write in a second. |
            | maxPageRate  | Maximum number of pages in and pages out that the job can perform in a second. |
          type: string
          enum:
          - maxMemory
          - maxCpuTime
          - maxClockTime
          - maxIoTotal
          - maxIoRate
          - maxPageRate
        value:
          description: The value of the resource that cannot be exceeded.
          type: number
          format: double
    queueTimeBasedParameters:
      title: Queue Time-Based Configuration
      description: Provides the parameters in a queue that can be changed by a time-based
        override.
      type: object
      properties:
        exchangeCreds:
          description: A flag that is the logical OR of the exchangeCreds flag on
            the job and the exchangeCreds flag on the queue. Valid in SAS v9 Windows
            only.
          type: boolean
          example: false
          deprecated: true
        isDefaultQueue:
          description: A flag that indicates whether this queue is the default queue.
            Only one queue can be the default queue at a time.
          type: boolean
        willRestartJobs:
          description: A flag that indicates that restartable jobs can be restarted
            in this queue.
          type: boolean
        priority:
          description: >-
            The priority value of this queue relative to other queues.
            A '-1' indicates to use the value in the default configuration.
          type: integer
          format: int32
          example: 35
          minimum: -1
          maximum: 2147483647
        maxJobs:
          description: >-
            The maximum number of jobs that can be running at any time from this queue.
            A '-1' indicates to use the value in the default configuration.
            A '0' indicates there is no limit.
          type: integer
          format: int64
          example: 12
          minimum: -1
          maximum: 10000
          default: 0
        maxJobsPerUser:
          description: >-
            The maximum number of jobs that can be running at any time from the same
            user.
            A '-1' indicates to use the value in the default configuration.
            A '0' indicates there is no limit.
          type: integer
          format: int64
          example: 4
          minimum: -1
          maximum: 10000
          default: 0
        maxJobsPerHost:
          description: >-
            The maximum number of jobs that can be running at any time on the same
            host.
            A '-1' indicates to use the value in the default configuration.
            A '0' indicates there is no limit.
          type: integer
          format: int64
          example: 8
          minimum: -1
          maximum: 10000
          default: 0
        scalingMinJobs:
          description: The number of jobs that must be in the pending state for the
            amount of time indicated by scalingMinSecs before a scaling request is
            made. Used only when scaled hosts exist and waiting might result in another
            job completing, which frees up an opportunity for a job to run. Ignored
            when scaled hosts do not exist because there is never an opportunity for
            a job to run when hosts do not exist. A '-1' indicates to use the value
            in the default configuration.
          type: integer
          format: int64
          example: 8
          minimum: -1
          maximum: 10000
          default: 0
        scalingMinSecs:
          description: The number of seconds a job must be in the pending state before
            being considered for a scaling request. Used only when scaled hosts exist
            and waiting might result in another job completing, which frees up an
            opportunity for a job to run. Ignored when scaled hosts do not exist because
            there is never an opportunity for a job to run when hosts do not exist.
            A '-1' indicates to use the value in the default configuration.
          type: integer
          format: int64
          example: 60
          minimum: -1
          maximum: 3600
          default: 0
        admins:
          description: The list of users and groups that can be the queue's administrators.
          type: array
          items:
            type: string
          example: ["queueAdmin", "queueAdminGroup"]
        users:
          description: The list of users and groups that can use the queue. If no
            users are specified, all users can use the queue.
          type: array
          items:
            type: string
          example: ["queueUser", "queueUserGroup"]
        hostNames:
          description: The list of host names on which this queue can schedule jobs
            to run.
          type: array
          items:
            type: string
          example: ["host1", "host2"]
        hostTypes:
          description: The list of host types on which this queue can schedule jobs
            to run.
          type: array
          items:
            type: string
          example: ["hostType1", "hostType2"]
        requiredTags:
          description: The list of tags required to be on hosts such that this queue
            can schedule jobs to the host.
          type: array
          items:
            type: string
          example: ["gpuEnabled", "fastIO"]
        consumedResources:
          description: >-
            An array of consumable static-resources values that must be available
            on a host before a job is scheduled to that host.
            After the job is scheduled, the resource values are decremented from the
            host's total.
          type: array
          items:
            $ref: "#/components/schemas/jobStaticResourceRequirement"
        requiredResources:
          description: >-
            An array of consumable static-resources values that must be available
            on a host before a job is scheduled to that host.
            The resource values are not decremented from the host's total.
          type: array
          items:
            $ref: "#/components/schemas/jobStaticResourceRequirement"
        limits:
          description: >-
            An array of consumable static-resources values that must be available
            on a host before a job is scheduled to that host.
            The resource values are not decremented from the host's total.
          type: array
          items:
            $ref: "#/components/schemas/jobResourceLimit"
        preempts:
          description: A list of queues that this queue can preempt if it has jobs
            to schedule and the grid has no openings to run a job from this queue.
          type: array
          items:
            type: string
          example: ["batch"]
    hostTypeTimeBasedParameters:
      title: Host Type Time-Based Configuration
      description: Provides the parameters in a host type that can be changed by a
        time-based override.
      type: object
      properties:
        maxJobsAllowed:
          description: |-
            The number of concurrent jobs that this host can run at a time:
            * If the value is greater than 0, that value is used.
            * If the value is less than 0, the absolute value of the number is multiplied by the number of CPU cores on the host.
            * If the value is 0, no jobs are allowed to run on that host."
          type: integer
          format: int32
          example: 5
          default: 250
          minimum: -250
          maximum: 10000
        minScalableNodes:
          description: |-
            The minimum number of scalable nodes that must be running for this host type.
            This field is ignored if the host type is not scalable.
            * If the default configuration is being parsed, and the value is either -1 or not specified, the value will be set to 0.
            * If a configuration other than the default is being parsed, and the value is either -1 or not specified, the default configuration's value is used.
          type: integer
          format: int32
          example: 5
          default: -1
          minimum: -1
          maximum: 100
        tags:
          description: Tags provided for scheduling.
          type: array
          items:
            type: string
        schedulingThresholds:
          description: When entered, the thresholds that prevent scheduling to hosts
            in this host type.
          type: array
          items:
            $ref: "#/components/schemas/hostTypeThreshold"
        suspendThresholds:
          description: When entered, the thresholds that cause jobs on hosts in this
            host type to be suspended.
          type: array
          items:
            $ref: "#/components/schemas/hostTypeThreshold"
    tenantQueueTimeBasedConfig:
      title: Tenant Queue Time-Based Overrides Configuration
      description: Provides a time-based queue configuration that can be modified
        by a tenant administrator.
      type: object
      allOf:
      - $ref: "#/components/schemas/tenantQueueTimeBasedInfo"
      - type: object
        properties:
          name:
            description: The name of the time-based configuration.
            readOnly: true
            example: "Night Time"
    jobEnvVar:
      title: Job Environment Variable
      description: Provides information about an environment variable.
      type: object
      properties:
        name:
          description: The name of the environment variable.
          type: string
          example: SAS_LOG_LEVEL
        value:
          description: The value of the environment variable.
          type: string
          example: DEBUG
    queueTimeBasedOverride:
      title: Queue Time-Based Overrides Configuration
      description: Provides the parameters in a queue that can be changed by a time-based
        override.
      type: object
      allOf:
      - $ref: "#/components/schemas/queueTimeBasedParameters"
      - type: object
        properties:
          name:
            description: The name of this time-based override.
            type: string
            example: "night-time"
          validIntervals:
            description: The array of time intervals that the time-based parameters
              can use.
            type: array
            items:
              $ref: "#/components/schemas/timeBasedIntervals"
    hostTypeTimeBasedOverride:
      title: Host Type Time-Based Overrides Configuration
      description: Provides the parameters in a host type that can be changed by a
        time-based override.
      type: object
      allOf:
      - $ref: "#/components/schemas/hostTypeTimeBasedParameters"
      - type: object
        properties:
          name:
            description: The name of this time-based override.
            type: string
            example: "night-time"
          validIntervals:
            description: The array of time intervals in which the time-based parameters
              are used.
            type: array
            items:
              $ref: "#/components/schemas/timeBasedIntervals"
    hostTypeThreshold:
      title: Host Type Threshold
      type: object
      description: Provides the start and end values for a metric that indicates a
        host is in a specified threshold.
      properties:
        metric:
          description: |-
            The name of the metric to check for this threshold.
            The metric can be any resource name, but generally is a predefined dynamic-resource name or
            any user-defined host-dynamic resource name. The predefined resource names are:
            |  **Field Name**  |        **Description**               | **Type** |
            |------------------|--------------------------------------|----------|
            |cpuRating         | The CPU rating of the host.          | static   |
            |cores             | The number of cores on the host.     | static   |
            |memory            | The total memory on the host.        | static   |
            |swap              | The total swap space on the host.    | static   |
            |temp              | The total temp space on the host.    | static   |
            |utilization       | The total utilization on the host.   | dynamic  |
            |runQueue15s       | The 15-second run queue on the host. | dynamic  |
            |runQueue1m        | The 1-minute run queue on the host.  | dynamic  |
            |runQueue15m       | The 15-minute run queue on the host. | dynamic  |
            |diskIORate        | The disk IO rate on the host.        | dynamic  |
            |netIORate         | The network IO rate on the host.     | dynamic  |
            |ioRate            | The total IO rate on the host.       | dynamic  |
            |pgRate            | The page rate on the host.           | dynamic  |
            |usedMemory        | The used memory on the host.         | dynamic  |
            |usedSwap          | The used swap space on the host.     | dynamic  |
            |usedTemp          | The used temp space on the host.     | dynamic  |
          type: string
        startOperator:
          description: The comparison operator that is used to enter the threshold.
          type: string
          enum:
          - "GT"
          - "LT"
          - "EQ"
          - "GE"
          - "LE"
          - ">"
          - "<"
          - "="
          - ">="
          - "<="
        startValue:
          description: The value to use for the comparison to enter the threshold.
          type: number
        endOperator:
          description: The comparison operator that is used to exit the threshold.
          type: string
          enum:
          - "GT"
          - "LT"
          - "EQ"
          - "GE"
          - "LE"
          - ">"
          - "<"
          - "="
          - ">="
          - "<="
        endValue:
          description: The value to use for the comparison to exit the threshold.
          type: number
    timeBasedIntervals:
      title: Time-Based Interval
      type: object
      description: Provides a start-time expression and end-time expression that specify
        when the values in this object override the default values.
      properties:
        start:
          description: The CRON expression representing when the time interval begins.
          type: string
          example: "* 1 * * *"
        end:
          description: The CRON expression representing when the time interval ends.
          type: string
          example: "* 2 * * *"
  responses:
    badHMACResponse:
      description: The request was missing the HMAC header or the HMAC was invalid.
      content:
        application/vnd.sas.error+json:
          schema:
            $ref: "#/components/schemas/error2"
          example:
            version: 2
            httpStatusCode: 400
            message: "The request content is not valid."
    notAcceptableResponse:
      description: The requested response type is not acceptable. See the returned
        error response for more information and acceptable response types.
      content:
        application/vnd.sas.error+json:
          schema:
            $ref: "#/components/schemas/error2"
          example:
            version: 2
            httpStatusCode: 406
  examples:
    apiInfoResponse:
      summary: SAS Workload Orchestrator API Info
      description: A list of all the top-level links provided by the SAS Workload
        Orchestrator.
      value:
        version: 1
        links:
        - method: PUT
          rel: daemonGridConfig
          href: "/workloadOrchestratorServer/daemons/grid"
          uri: "/workloadOrchestratorServer/daemons/grid"
          type: application/vnd.sas.sasgrid.daemon.config
        - method: PUT
          rel: daemonTenantConfig
          href: "/workloadOrchestratorServer/daemons/grid"
          uri: "/workloadOrchestratorServer/daemons/grid"
          type: application/vnd.sas.sasgrid.grid.tenant.config
        - method: POST
          rel: createJob
          href: "/workloadOrchestratorServer/jobs"
          uri: "/workloadOrchestratorServer/jobs"
          type: application/vnd.sas.sasgrid.job
    daemonGridConfig:
      summary: Daemon Grid Configuration Example
      description: An example of the grid configuration information sent to a server
        or backup manager along with a verification hash.
      value:
        configuration:
          $ref: "#/components/examples/gridConfig"
        hash: "3a7bd3e2360a3d80f8b1d1c1d6f9c4e5b3c6e0f1e2d3c4b5a6b7c8d9e0f1a2b"
    daemonHostInfo:
      summary: Daemon Host Information Example
      description: An example of the information about a host that is being added
        to the grid.
      value:
        version: 4
        name: "host1.mycompany.com"
        aliases: ["host1", "h1"]
        ipAddresses: ["10.10.2.4"]
        tenantName: "acme"
        k8sPodName: "sas-workload-orchestrator-server-xed5f"
        k8sNamespace: "d33887"
        appPlaneURLFmt: "https://@k8s.service.name@"
        k8sIngressURL: "https://d33887.contour.host1.mycompany.com"
        k8sInternalTLS: true
        k8sInternalPort: 8080
        staticInfo:
          version: 1
          cpuRating: 2000.125
          cores: 4.125
          memory: 8192.125
          swap: 2048.125
          temp: 10240.125
          startTime: "2023-10-01T12:00:00Z"
          buildDate: "2023-10-01T12:00:00Z"
          osName: "Linux"
          osDesc: "Red Hat Enterprise Linux 9"
          cpuVendor: "Intel"
          cpuArch: "x64"
          podMemory: 12000.125
          podCores: 12
          properties: ["workload.sas.com/class=compute"]
          allocatables:
            nvidia.gpu: 10.125
            bigdata.sas.com/spark: 5
    daemonHostState:
      summary: Daemon Host State Example
      description: An example of the state information about a host that is being
        added to the grid.
      value:
        version: 1
        name: "host1.mycompany.com"
        dynamicInfo:
          version: 4
          utilization: 75.5
          runQueue15s: 2.5
          runQueue1m: 3.125
          runQueue15m: 4.125
          diskIORate: 1500.125
          netIORate: 3000.125
          ioRate: 4500.125
          pgRate: 20.125
          usedMemory: 4096.125
          usedSwap: 1024.125
          usedTemp: 5120.125
          reservedMemory: 1024.125
          reservedCores: 1.125
          jobsRunning: 5
          jobsSuspendedAdmin: 1
          jobsSuspendedPreempt: 0
          jobsSuspendedThreshold: 0
          licenseValid: true
          licenseType: 1
          licenseExpireDate: "2024-12-31"
          hostCordoned: false
          hostUpdateTime: "2023-10-01T12:30:00Z"
          hostInDiskPressure: true
          hostInMemoryPressure: false
          hostInPIDPressure: false
          hostNetworkUnavailable: false
    jobInfo:
      summary: Job Information
      description: Provides all information about a job, including job request information
        and job processing information.
      value:
        version: 1
        id: 5
        request:
          name: RUNSASLM
          queue: default
          project: ""
          user: user1
          tenant: uaa
          startAfter: "2022-08-11T18:30:00Z"
          restartable: false
          exchangeCreds: false
          type: KUBERNETESJOB
          cmd: ""
          args: []
          envVars: []
          k8sJobDef: '{"apiVersion":"batch/v1","kind":"Job", ..., {"configMap": {"defaultMode":
            420,"name": "sas-customer-provided-ca-certificates-km44fc867t"},"name":
            "customer-provided-ca-certificates"}]}}}}}'
          k8sNamespace: d33887
          k8sSecrets:
          - SAS_SERVICES_REFRESH_TOKEN
          - SAS_SERVICES_TOKEN
          - SAS_CLIENT_TOKEN
          hosts: []
          requiredTags: []
          consumedResources:
          - name: cores
            value: 0.15
          - name: memory
            value: 536.102295
          requiredResources: []
          limits: []
          properties: []
        processingInfo:
          state: COMPLETED
          pendingCause: ""
          hostSchedulingInfo:
          - hostName: host-node1
            state: HOST-STATE-AVAILABLE
          - hostName: host-node2
            state: HOST-STATE-AVAILABLE
          - hostName: host-node3
            state: HOST-STATE-AVAILABLE
          - hostName: host-node4
            state: HOST-STATE-AVAILABLE
          exchangeCreds: false
          queue: default
          userGroup: ""
          suspendFlags: 0
          queuePriority: 10
          submitTime: "2022-08-11T18:30:21Z"
          startTime: "2022-08-11T18:30:21Z"
          endTime: "2022-08-11T18:32:21Z"
          lastCheckTime: "2022-08-12T17:26:21Z"
          launchStatus: 0
          processId: 125
          k8sObjName: sas-batch-server-c6c76a87-f12a-4d39-aa8b-c945c389c06f-5
          requeueCount: 0
          executionIP: "10.254.3.220"
          executionHost: host-node1
          executionHostAliases:
          - sas-workload-orchestrator-stz2w
          exitCode: 0
          totalTimePending: 0
          totalTimeRunning: 82560
          totalTimeSuspAdmin: 0
          totalTimeSuspThresh: 0
          totalTimeSuspPreempt: 0
          hosts: []
          requiredTags: []
          consumedResources:
          - name: cores
            value: 0.15
          - name: memory
            value: 536.102295
          requiredResources: []
          limits: []
          limitValues:
          - name: maxMemory
            value: 0.00075
          - name: maxCpuTime
            value: 0.2
          - name: maxClockTime
            value: 82551.332392
          - name: maxIoTotal
            value: 2.710938
          - name: maxIoRate
            value: 17960.87668
          - name: maxPageRate
            value: 0
          stdOutput: []
    loggersLevelInfo:
      summary: Loggers Information
      description: Provides loggers and their levels for a specific host.
      value:
        version: 1
        hostName: sas-workload-orchestrator-0
        loggers:
        - name: root
          level: INFO
        - name: App
          level: "NULL"
        - name: Admin
          level: "NULL"
    gridConfig:
      summary: Grid Configuration
      description: Provides an operational configuration of the grid.
      value:
        version: 3
        name: ""
        description: ""
        jobChangeCmd: ""
        port: 8080
        userNamesNoCase: false
        hostInactiveSecs: 60
        hostUnknownSecs: 900
        hostUpdateSecs: 15
        jobPurgeSecs: 3600
        jobUpdateSecs: 15
        jobLifetimeDays: 60
        jobLifetimeCount: 20000
        managerUpdateSecs: 15
        roundRobinSecs: 0
        schedulerUpdateSecs: 15
        scalingRequestSecs: 0
        scalingHostWaitSecs: 600
        scalingHostUpSecs: 300
        maxStdOutLines: 50
        userResources: []
        compareOrder: []
        admins:
        - SASAdministrators
        tenantAdmins: []
        hostTypes:
        - version: 1
          name: default
          description: SAS Workload Orchestrator Server Hosts on Kubernetes Nodes
          role: server
          scalable: false
          externalURL: ""
          hostNames: []
          hostNamePatterns: []
          hostIpPatterns: []
          hostIpRanges: []
          hostProperties: []
          maxJobsAllowed: -1
          minScalableNodes: 0
          tags: []
          scheduleThresholds: []
          suspendThresholds: []
          timeBasedOverrides: []
        queues:
        - name: default
          description: ""
          tenant: uaa
          isDefaultQueue: true
          willRestartJobs: false
          exchangeCreds: false
          priority: 10
          maxJobs: -1
          maxJobsPerUser: -1
          maxJobsPerHost: -1
          scalingMinJobs: -1
          scalingMinSecs: -1
          users: []
          admins: []
          hostNames: []
          hostTypes: []
          requiredTags: []
          consumedResources: []
          requiredResources: []
          limits: []
          preempts: []
          timeBasedOverrides: []
        userGroups: []
        hostGroups: []
    loggersLevelRequest:
      summary: Loggers Level Request
      description: Requests to set the loggers to specific levels for specific hosts.
      value:
        version: 1
        hosts:
        - sas-workload-orchestrator-0
        - sas-workload-orchestrator-1
        loggers:
        - name: root
          level: INFO
        - name: App
          level: "NULL"
        - name: Admin
          level: "NULL"
    logsCollection:
      summary: Log Line Collection
      description: Provides requested log lines in SAS collection format.
      value:
        version: 2
        accept: application/vnd.sas.sasgrid.log.line
        name: lines
        items:
        - logger: App.Grid.SGMG
          level: INFO
          msg: "[00000005] sas -  "
          dateTime: "2022-08-08T02:14:28Z"
        - logger: App.Grid.SGMG
          level: INFO
          msg: "[00000005] sas - SAS Workload Orchestrator Daemon - Version 4.0 (2022-08-01T22:12:01Z)"
          dateTime: "2022-08-08T02:14:28Z"
        count: 2
        start: 0
        limit: 30
        links:
        - href: "/workloadOrchestrator/logs"
          method: GET
          rel: self
          uri: "/workloadOrchestrator/logs"
        - href: "/workloadOrchestrator/logs?START=30&LIMIT=30"
          method: GET
          rel: next
          uri: "/workloadOrchestrator/logs?START=30&LIMIT=30"
        - href: "/workloadOrchestrator/logs?START=0&LIMIT=30"
          method: GET
          rel: first
          uri: "/workloadOrchestrator/logs?START=0&LIMIT=30"
        - href: "/workloadOrchestrator/logs?START=2&LIMIT=30"
          method: GET
          rel: last
          uri: "/workloadOrchestrator/logs?START=2&LIMIT=30"
    tenantConfig:
      summary: Tenant Configuration
      description: Provides a grid configuration specific to a particular tenant.
      value:
        version: 1
        tenant: acme
        admins:
        - Acme-Tenant-Admin1
        - Acme-Tenant-Admin2
        queues:
        - name: default
          description: "Default queue for default provider"
          tenant: acme
          users: ["acme-user1", "acme-user2"]
          admins: []
          timeBasedOverrides:
          - name: "Night Time"
            users:
            - "acme-user1"
            - "acme-user2"
            - "acme-user3"
            admins: []
        userGroups:
        - name: myTenantGroup1
          tenant: acme
          admins:
          - myTenantUserGroup1Admins
          - acme-user1
    logsInfo:
      summary: Log Line Information Array
      description: Provides requested log lines in the original SAS Workload Orchestrator
        format.
      value:
        version: 1
        lines:
        - logger: App.Grid.SGMG
          level: INFO
          msg: "[00000005] sas -  "
          dateTime: "2022-08-08T02:14:28Z"
        - logger: App.Grid.SGMG
          level: INFO
          msg: "[00000005] sas - SAS Workload Orchestrator Daemon - Version 4.0 (2022-08-01T22:12:01Z)"
          dateTime: "2022-08-08T02:14:28Z"
  parameters:
    logsLogger:
      name: logger
      description: The name of the logger that a log line must have so it can be returned
        in the collection.
      in: query
      required: false
      schema:
        type: string
    logsHost:
      name: host
      description: The short host name of the host from which to query log lines.
      in: query
      required: false
      schema:
        type: string
    logsThreshold:
      name: threshold
      description: The name of the logger that a log line must have (at a minimum)
        to be returned in the collection.
      in: query
      required: false
      schema:
        type: string
        enum:
        - TRACE
        - DEBUG
        - INFO
        - WARN
        - ERROR
        - FATAL
    logsEndTime:
      name: endTime
      description: The latest time that a log line must be returned in the collection.
      in: query
      required: false
      schema:
        type: string
    logsStartTime:
      name: startTime
      description: The earliest time that a log line must be returned in the collection.
      in: query
      required: false
      schema:
        type: string
    collectionStart:
      name: start
      description: The starting-item offset of the items returned in the collection.
        This is available only when the return type is application/vnd.sas.collection.
      in: query
      required: false
      schema:
        type: integer
        default: 0
        minimum: 0
    collectionLimit:
      name: limit
      description: The maximum number of items returned in the collection. This is
        available only when the return type is application/vnd.sas.collection.
      in: query
      required: false
      schema:
        type: integer
        default: 10
        maximum: 1000
    logsLevel:
      name: level
      description: The logging level that a log line must have so it can be returned
        in the collection.
      in: query
      required: false
      schema:
        type: string
        enum:
        - TRACE
        - DEBUG
        - INFO
        - WARN
        - ERROR
        - FATAL






